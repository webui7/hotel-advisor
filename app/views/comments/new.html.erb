<%title "New hotel"%>
<div class="row">
<h1>New comment</h1>
<%= link_to 'Back to hotel', hotel_path(@comment.hotel) , :class=> "button" %>
<%= form_for(@comment, :url => hotel_comments_path(@comment.hotel),  html:{:class => 'custom'}) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this hotel from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%= render 'comments/form' , :f=> f %>
  <div class="actions">
    <%= f.submit 'Submit',  :class => 'button round' %>
  </div>
<%end %>
</div>